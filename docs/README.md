# docs

Documentation assets for the project.

- **`dashboard.png`** — the README hero screenshot (a v1 close gate). Capture it
  from the **demo instance** (`str-host-dashboard-demo`), which serves dummy data
  with no guest PII, and save it here as `dashboard.png`. Embedded near the top of
  the root `README.md`.
